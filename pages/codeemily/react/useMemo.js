import ReactCodeBlock from  '../../../components/ReactCodeBlock';

const useMemo = () => {
  return (
    <div>
      useMemo
        <ReactCodeBlock
            code={
              `() => {}
               () => {}`
            } />
    </div>
  )
}

export default useMemo;